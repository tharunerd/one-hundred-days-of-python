this is some random text


---

## ✅ Key Learnings:

- Using `with open()` for safe file access
- Stripping `\n` from each name using `.strip()`
- Efficient string replacement with `.replace()`
- Basic automation using file input/output

---

## 🧠 Challenge Yourself:

- Add error handling (e.g., if a file doesn't exist).
- Make it accept dynamic template and name file input.
- Export letters as PDFs using external libraries like `fpdf`.

---

## 🚀 What's Next (Day 25):
Learn how to work with Python's CSV module to read, write, and analyze structured data.

---

